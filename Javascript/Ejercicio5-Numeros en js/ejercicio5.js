const alturaCm = 154;
const alturaM = 1.54;
const pesokg = 60.1;
const alturaRedondeada = Math.ceil(alturaM);
const pesoRedondeado = Math.floor(pesokg);
const max_value_js = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
console.log(max_value_js);
